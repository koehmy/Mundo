/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['ogjsblupoteatefyizgn.supabase.co', 'images.unsplash.com'],
  },
};

export default nextConfig;
